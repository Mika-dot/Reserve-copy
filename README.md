# Поток копирующий папку с подпапками

Добавляем пространство имен
```C#
  using TransferStream;
```

Для воспользования указываем путь первой папки и папки куда копируем
```C#
  CopyStream.CopyStreamFolder(new string[] { @"Путь", @"Путь" });
```
